syntax = "proto3";

package mapreduce;

import "common.proto";

option go_package = "pkg/maprecude/api/v1";

service CoordinatorService {
  rpc ServerLive(ServerLiveRequest) returns (ServerLiveResponse) {}
}
